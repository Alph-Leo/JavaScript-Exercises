let log = console.log
let x1 = 3
let y1 = 10
let x2 = 8
let y2 = 7;
    var xDistance = Math.sqrt(Math.pow(x2 - x1, 2));
    var yDistance = Math.sqrt(Math.pow(y2 - y1, 2));
    var distance = xDistance + yDistance;
  
  log(`The distance between: ${distance}`);
  