let string = "codeleet";
let arr = [4, 5, 6, 7, 0, 1, 2, 3];
let output = ' ';
for(let index = 0; index < arr.length; index++){
output += string[arr[index]];
}
console.log(output);

let strings = "someiclon"
let arrays = [0, 3, 2, 4, 5, 1, 6, 7, 8];
let originalWord = ' ';
for(let index = 0; index < arrays.length; index++){
    originalWord += strings[arrays[index]]; 
}
console.log(originalWord);