const prompt = require("prompt-sync")();
let dataSet = [[1,2,3],[4,5,6],[7,8,9]];

let row = prompt("enter row length ");
let column = prompt("Enter column length ");
for(let i = 0; i <= row; i++){
    for(j = 0; j <= column ; j++){

    }
}
// console.table(dataSet.slice[row][column]);
console.table(dataSet[2].slice(0,1));